# FPDesigner — local development environment
# Copy to .env.local for Next.js. Never commit real secrets.
# B1-P0: foundation only. No Supabase Auth / Factory credentials yet.

APP_ENV=local
NEXT_PUBLIC_APP_ENV=local
